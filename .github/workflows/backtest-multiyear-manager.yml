name: Multi-year fantasy manager walk-forward

on:
  pull_request:
    paths:
      - 'scripts/backtest_multiyear_manager.py'
      - 'config/season_management_v2.json'
      - 'js/season-management-intelligence.js'
      - '.github/workflows/backtest-multiyear-manager.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run 2021-2025 walk-forward replay
        run: python scripts/backtest_multiyear_manager.py data/backtests/walkforward
      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: walkforward-manager-backtest
          path: data/backtests/walkforward/
